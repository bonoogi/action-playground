name: just print action

on:
  workflow_dispatch:
  push:
    tags:
      - '*'
    branches:
      - 'main'

jobs:
    just_print_action:
        name: Print Foo!
        runs-on: ubuntu-latest

        steps:
            - name: print
              run: |
                echo "Hello World!"